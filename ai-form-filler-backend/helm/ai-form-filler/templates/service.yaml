apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-form-filler.fullname" . }}
  labels:
    {{- include "ai-form-filler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-form-filler.selectorLabels" . | nindent 4 }}
